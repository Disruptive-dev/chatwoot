name: DEPRECATED — Build Optimia Chatwoot Image

# Replaced by optimia-build-production.yml (v0.2.1 CI/CD cleanup).
# This workflow no longer builds or pushes images.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow deprecated
        run: |
          echo "::error::This workflow was replaced by .github/workflows/optimia-build-production.yml"
          echo "Production builds require manual workflow_dispatch with environment approval."
          echo "Push to develop no longer triggers production image builds."
          exit 1
