name: DEPRECATED — Build Optimia Chatwoot Staging Image

# Replaced by optimia-build-staging.yml (v0.2.1 CI/CD cleanup).

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow deprecated
        run: |
          echo "::error::This workflow was replaced by .github/workflows/optimia-build-staging.yml"
          echo "Use workflow_dispatch or push tag staging-cw-* on the new workflow."
          exit 1
